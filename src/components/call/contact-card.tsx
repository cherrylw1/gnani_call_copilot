import { Card } from "@/components/ui/card";
export function ContactCard({ contact }: { contact: Record<string, unknown> }) { return <Card className="p-4"><h2 className="text-xs font-medium uppercase tracking-wide text-zinc-500">Contact</h2><p className="mt-3 font-medium">{String(contact.full_name || "Unknown contact")}</p><p className="mt-1 text-sm text-zinc-400">{String(contact.job_title || "Title unknown")}</p><dl className="mt-4 grid gap-2 text-sm"><Info label="Email" value={contact.email} /><Info label="Phone" value={contact.work_phone} /><Info label="Location" value={[contact.city, contact.state, contact.country].filter(Boolean).join(", ")} /><Info label="Persona" value={contact.persona} /></dl></Card>; }
function Info({ label, value }: { label: string; value: unknown }) { return <div><dt className="text-xs text-zinc-500">{label}</dt><dd className="mt-0.5 break-words text-zinc-300">{String(value || "-")}</dd></div>; }
