import { Card } from "@/components/ui/card";
export function SignalCard({ signals }: { signals: Record<string, unknown> | null }) { const rows: [string, unknown][] = [["Priority", signals?.priority_score_normalized], ["Call volume", signals?.call_volume_band_raw], ["Budgeting", signals?.budgeting_period_raw], ["Voice / speech AI", signals?.investing_voice_ai_raw], ["Chatbots", signals?.investing_chatbots_raw], ["Conversational IVR", signals?.investing_conversational_ivr_raw], ["Tech partner", signals?.no_right_tech_partner_raw], ["APAC outsourcing", signals?.outsourcing_apac_raw], ["PDF confidence", signals?.pdf_match_confidence_raw]]; return <Card className="p-4"><h2 className="text-xs font-medium uppercase tracking-wide text-zinc-500">Signals</h2><div className="mt-3 grid gap-2 sm:grid-cols-2">{rows.map(([label, value]) => <div key={label} className="border border-zinc-800 p-2"><p className="text-[11px] text-zinc-500">{label}</p><p className="mt-1 text-sm text-zinc-200">{String(value || "-")}</p></div>)}</div></Card>; }
