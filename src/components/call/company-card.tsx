import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
export function CompanyCard({ company, onResearch, loading }: { company: Record<string, unknown> | null; onResearch: () => void; loading: boolean }) { return <Card className="p-4"><div className="flex items-start justify-between gap-3"><div><h2 className="text-xs font-medium uppercase tracking-wide text-zinc-500">Company</h2><p className="mt-3 font-medium">{String(company?.company_name || "Company unknown")}</p></div>{Boolean(company?.id) && <Button className="bg-zinc-900 text-zinc-100 hover:bg-zinc-800" onClick={onResearch} disabled={loading}>{loading ? "Refreshing..." : "Refresh research"}</Button>}</div><p className="mt-2 text-sm text-zinc-400">{String(company?.industry_auto_classified || "Industry unknown")}</p><dl className="mt-4 grid gap-2 text-sm"><div><dt className="text-xs text-zinc-500">Domain</dt><dd>{String(company?.domain || "-")}</dd></div><div><dt className="text-xs text-zinc-500">Status</dt><dd>{String(company?.buyer_partner_competitor_status || "Needs review")}</dd></div>{company?.research_summary ? <div><dt className="text-xs text-zinc-500">Research summary</dt><dd className="mt-1 text-zinc-300">{String(company.research_summary)}</dd></div> : null}</dl></Card>; }
