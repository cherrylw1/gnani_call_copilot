export function StatusBadge({ children, tone = "neutral" }: { children: React.ReactNode; tone?: "neutral" | "good" | "warn" }) { const toneClass = tone === "good" ? "border-white bg-white text-black" : tone === "warn" ? "border-zinc-500 text-zinc-200" : "border-zinc-700 text-zinc-300"; return <span className={`inline-flex border px-2 py-0.5 text-[11px] font-medium ${toneClass}`}>{children}</span>; }
